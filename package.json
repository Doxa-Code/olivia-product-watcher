{
  "name": "product-watcher",
  "version": "2.0.0",
  "scripts": {
    "dev": "bun run watcher.ts",
    "config": "bun run config-wizard.ts",
    "build": "bun build --compile --minify watcher.ts --outfile dist/watcher",
    "build:linux": "bun build --compile --minify --target=bun-linux-x64 watcher.ts --outfile dist/watcher-linux"
  },
  "dependencies": {
    "@seald-io/nedb": "^4.1.2",
    "axios": "^1.13.2",
    "mysql": "2.0.0",
    "pg": "^8.16.3",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/mysql": "^2.15.27",
    "@types/pg": "^8.16.0",
    "@types/prompts": "^2.4.9"
  }
}
